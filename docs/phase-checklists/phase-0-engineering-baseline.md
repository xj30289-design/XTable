# Phase 0 测试评估验收清单：工程骨架与技术基线

## 自动化测试

- `已关闭` 应用入口可导入并启动。
- `已关闭` 主窗口可创建，核心对象名稳定。
- `已关闭` 顶部工具栏、中间工作区、左侧功能栏、底部状态栏存在。
- `已关闭` Light/Dark 基础主题样式可应用。
- `已关闭` pytest 测试命令可执行。

## 评估项目

- `已关闭` `app` 与 `ui` 模块边界已收敛，主 UI 细节进入 `xtable.ui`。
- `已关闭` 主题 token 覆盖工具栏、菜单、状态栏、提示和禁用/悬停/选中状态。
- `已关闭` Phase 0 无未关闭 P1 问题。

## 人工验收

- `已关闭` 双击或命令行启动应用后主窗口正常显示。
- `已关闭` 主窗口基础区域边界清晰。
- `已关闭` 浅色/深色主题下基础控件可读。
