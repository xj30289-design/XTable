# Phase 5 测试评估验收清单：导入导出

## 自动化测试

- `未开始` CSV 编码、表头、空值和基础类型解析测试。
- `未开始` Excel 多 sheet 导入测试。
- `未开始` 普通表、分组表、二维表专属导出规则测试。
- `未开始` 导入失败报告测试。
- `未开始` 导出前校验阻塞测试。
- `未开始` 类型与规则定义导出测试。

## 评估项目

- `未开始` 导入导出模块不得承载业务规则判断。
- `未开始` 导出前必须复用 Phase 4 校验结果。
- `未开始` 导入失败需要能进入日志与诊断系统。

## 人工验收

- `未开始` 真实 Excel/CSV 文件导入验收。
- `未开始` 当前表导出和项目导出验收。
- `未开始` 导入导出失败提示和日志验收。
