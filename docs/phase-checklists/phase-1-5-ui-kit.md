# Phase 1.5 测试评估验收清单：Editor UI Demo 与 UI Kit 基座

## 自动化测试

- `已关闭` `python -m xtable.ui.demo` Demo 入口可创建。
- `已关闭` UI Kit 组件边界和 UI-first 架构门禁通过。
- `已关闭` 图标按钮、导航、工具栏、状态栏、诊断抽屉、表格样板、字段面板和弹窗样板可创建。
- `已关闭` Light/Dark 主题覆盖弹窗、菜单、工具栏、表格、输入控件和诊断抽屉。
- `已关闭` 焦点管理、页面切换失活、只读控件区分、表格快捷键和 Json 工具按钮有回归测试。
- `已关闭` 跨阶段问题 `X-003`：剪贴板快捷键测试已复验通过，全量回归恢复绿色。

## 评估项目

- `已关闭` Demo 控件完整度满足后续业务 UI 复用基座要求。
- `已关闭` 文本按钮治理和图标资源体系已建立。
- `已关闭` 输入焦点、表格能力、多页签、Json 编辑器和数据列表已形成可复用样板。

## 人工验收

- `待验收` 真实字体环境下重新截图确认中文显示。
- `待验收` Light/Dark 真实窗口视觉复验。
- `待验收` 手动确认 Demo 中表格复制/粘贴、Json 格式化、页签切换和只读状态。
