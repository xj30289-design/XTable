# Phase 2 测试评估验收清单：核心数据模型

## 自动化测试

- `已关闭` domain 模型不依赖 PySide6。
- `已关闭` 普通表、分组表、二维表可表达。
- `已关闭` 字段可引用 Enum、Meta 和 Table。
- `已关闭` 非法引用、非法表参数、Enum/Meta 结构、行结构和字段参数错配会被拒绝。
- `已关闭` schema 字典往返、未知类型拒绝、版本拒绝、Meta 循环阻塞、Table 循环发现通过。
- `已关闭` schema 文件保存、加载、缺失返回空结构、外部修改冲突和空 digest 基线保护通过。
- `已关闭` 引用索引与依赖查询测试覆盖 Enum、Meta、Table、Field。
- `已关闭` 默认值生成和新增行模板测试。
- `已关闭` 字段/表结构变更影响检查测试。
- `已关闭` 模型复制与引用重定向测试。
- `已关闭` schema 迁移入口测试。

## 评估项目

- `已关闭` 第八批次结构校验问题已关闭。
- `已关闭` 第九批次 schema 持久化问题已关闭。
- `已关闭` Phase 2 剩余开发任务已补齐自动化测试，并已通过测试角色验收。

## 人工验收

- `已关闭` 人工复核 Phase 2 验收标准与自动化测试映射。
- `已关闭` 确认 Phase 3 可基于当前模型进入表格编辑工作台开发。
